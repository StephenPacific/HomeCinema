{
  "name": "home-cinema-lan-sync",
  "version": "0.1.0",
  "private": true,
  "description": "LAN music sync player for multiple home devices.",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "test": "node --test",
    "start": "npm run build && node server.js",
    "serve": "node server.js",
    "dev": "npm run build && node server.js",
    "plugin:signal": "node plugin-cloud/server.js",
    "plugin:probe": "node scripts/probe-plugin-cloud.js",
    "plugin:preview-room": "node scripts/hold-plugin-room.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "lucide-react": "^1.17.0",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "vite": "^8.1.4"
  },
  "devDependencies": {
    "ajv": "^8.20.0"
  }
}
